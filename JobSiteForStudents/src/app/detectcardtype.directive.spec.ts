import { DetectcardtypeDirective } from './detectcardtype.directive';

describe('DetectcardtypeDirective', () => {
  it('should create an instance', () => {
    const directive = new DetectcardtypeDirective();
    expect(directive).toBeTruthy();
  });
});
